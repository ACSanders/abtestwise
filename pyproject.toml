[build-system]
requires = ["hatchling>=1.30.1,<1.32"]
build-backend = "hatchling.build"

[project]
name = "abtestwise"
version = "0.3.0"
description = "Lightweight toolkit for frequentist and Bayesian binary A/B experiment analysis."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["ab-testing", "experimentation", "statistics", "bayesian", "frequentist"]
dependencies = [
"numpy>=1.22",
"scipy>=1.11",
"matplotlib>=3.5",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pandas>=1.5",
]

[tool.hatch.build.targets.sdist]
include = [
"/src/abtestwise",
"/tests",
"/README.md",
"/pyproject.toml",
]
exclude = [
"/.venv",
"/.pypitest",
"/.testpypi",
"/.wheeltest",
"/.abtestwise-testpypi",
"/.abtestwise-testpypi-011",
"/.abtestwise-pypi",
"/.pytest_cache",
"/dist",
"/build",
"*.egg-info",
]

[tool.hatch.build.targets.wheel]
packages = ["src/abtestwise"]

[tool.pytest.ini_options]
testpaths = ["tests"]
